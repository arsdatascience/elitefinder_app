# Frontend environment variables for Docker
# These are passed to the frontend during build and runtime

# WAHA API endpoint (produção via Caddy subdomínio)
VITE_WAHA_BASE=https://marketsharedigital.com.br
VITE_WAHA_API_KEY=qR7sP!xZ2tYh*VbK6mN9eWdJ4rFaGcC8lQzE3yUo

# N8N endpoint (produção via Caddy subdomínio)
VITE_N8N_BASE=https://n8n.marketsharedigital.com.br

# N8N Analytics Metrics Endpoint (apex via Caddy) - Overall metrics
VITE_N8N_METRICS_ENDPOINT=https://n8n.marketsharedigital.com.br/webhook/api/analytics/metrics

# N8N Analytics Metrics Extended Endpoint (apex via Caddy) - Time/abandonment/distribution
VITE_N8N_METRICS_EXTENDED_ENDPOINT=https://n8n.marketsharedigital.com.br/webhook/api/analytics/metrics-extended

# N8N Manual Analysis Endpoint (apex via Caddy) - Manual quality analysis trigger
VITE_N8N_ANALISE_MANUAL_ENDPOINT=https://n8n.marketsharedigital.com.br/webhook/analise-manual

# N8N Conversations CSV Export Endpoint
VITE_N8N_CONVERSATIONS_EXPORT_ENDPOINT=https://marketsharedigital.com.br/webhook/api/conversations/export

# Analytics/Backend API (Python - not used in Docker)
VITE_ANALYTICS_API_BASE=http://localhost:5001

# Node.js Backend API (for analytics export via Caddy proxy)
VITE_BACKEND_URL=https://marketsharedigital.com.br
